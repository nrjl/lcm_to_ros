add_executable(@MESSAGE_TYPE@_republisher @MESSAGE_TYPE@_republisher.cpp)
target_link_libraries( @MESSAGE_TYPE@_republisher
    ${LCM_LIBRARY}
    ${catkin_LIBRARIES}
)
add_dependencies(@MESSAGE_TYPE@_republisher 
    ${${PROJECT_NAME}_EXPORTED_TARGETS} 
    ${catkin_EXPORTED_TARGETS}
)


